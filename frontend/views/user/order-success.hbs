{{!-- <section>
    <div class="flex flex-col items-center pt-32 h-4/5 px-5">
        <div>
            <h1 class="font-bold text-2xl lg:text-3xl text-center">Order Placed Successfully</h1>
        </div>
        <div class="mt-3">
            <a href="/orders"
                class="flex w-full items-center justify-center rounded-md bg-blue-600 px-5 py-2.5 text-sm font-medium text-white hover:bg-blue-800">View
                Orders</a>
        </div>
    </div>
</section> --}}

<section>
    <section class="bg-gray-50">
        <div class="flex flex-col items-center py-20 px-5 min-h-screen">
            <!-- Success Icon -->
            <div class="mb-6 p-4 bg-green-100 rounded-full">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-16 w-16 text-green-600" fill="none" viewBox="0 0 24 24"
                    stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                </svg>
            </div>

            <!-- Success Message -->
            <div class="text-center max-w-2xl">
                <h1 class="font-bold text-2xl lg:text-3xl mb-3">Order Placed Successfully!</h1>
                <p class="text-gray-600 mb-6">Your order has been confirmed and will be processed shortly.</p>
                 {{!-- We've sent
                    the order details to your email --}}

                <!-- Order Summary Card -->
                <div class="bg-white p-6 rounded-lg shadow-md mb-8 text-left">
                    <h2 class="font-semibold text-lg mb-4 border-b pb-2">Order Summary</h2>
                    <div class="flex justify-between mb-2">
                        <span class="text-gray-600">Order Number:</span>
                        <span class="font-medium">#ORD-{{orderId}}</span>
                    </div>
                    {{!-- <div class="flex justify-between mb-2">
                        <span class="text-gray-600">Estimated Delivery:</span>
                        <span class="font-medium">{{deliveryDate}}</span>
                    </div> --}}
                    <div class="flex justify-between mb-2">
                        <span class="text-gray-600">Payment Method:</span>
                        <span class="font-medium">{{paymentMethod}}</span>
                    </div>
                    <div class="flex justify-between mt-4 pt-4 border-t">
                        <span class="text-gray-600">Total Amount:</span>
                        <span class="font-bold text-lg">₹{{totalAmount}}</span>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a href="/orders"
                        class="flex items-center justify-center rounded-md bg-blue-600 px-5 py-3 text-sm font-medium text-white hover:bg-blue-800 transition-colors">
                        View Orders
                    </a>
                    <a href="/"
                        class="flex items-center justify-center rounded-md border border-gray-300 px-5 py-3 text-sm font-medium text-gray-700 hover:bg-gray-100 transition-colors">
                        Continue Shopping
                    </a>
                </div>

                <!-- Support Info -->
                {{!-- <div class="mt-8 text-sm text-gray-500">
                    <p>Need help? <a href="/contact" class="text-blue-600 hover:underline">Contact our support team</a>
                    </p>
                    <p class="mt-1">We'll email you an order confirmation with details.</p>
                </div> --}}
            </div>
        </div>
    </section>
</section>